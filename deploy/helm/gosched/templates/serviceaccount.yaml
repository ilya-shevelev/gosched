{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (printf "%s-gosched" .Release.Name) }}
  labels:
    app.kubernetes.io/name: gosched
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
